# ISCF Main Figure ImageGen Refinement Prompt v2

## Figure contract

**Core conclusion.** One encoded history and one explicit high-dimensional future-coordinate field construct a scope-indexed forecasting field, which target-conditioned scope probabilities contract into one final trajectory.

**Archetype.** Single-canvas AI-generated mechanism schematic; composition draft only.

**Invariant structure.** Preserve the v1 two-lane left-to-right layout, five aligned scope rows, exact stage order, exact labels and single weighted-contraction merge. Do not add BSCA, losses, requested horizons, experimental values or extra modules.

**Refinement target.** Improve the visual vocabulary for palette, time-series curves, future-coordinate geometry and intermediate tensors without changing the method semantics.

## ImageGen edit prompt

```text
Use case: precise-object-edit
Asset type: refined architecture schematic for a top-tier time-series forecasting paper
Input image: Image 1 is the edit target and structural reference.

Primary request: Refine Image 1 into a more sophisticated publication-style scientific architecture diagram. Preserve its complete left-to-right topology, two-lane hierarchy, five scope rows, stage order, arrow directions, merge point, white background and all existing labels. Change only the color system and the graphical representation of curves, future coordinates and intermediate latent variables. Do not redesign the architecture.

Color system: replace the current saturated primary/rainbow palette with one restrained low-saturation Nature Machine Intelligence-style family. Use deep indigo #484878 for history and structural anchors; medium periwinkle #7884B4 and pale periwinkle #B4C0E4 for history-conditioned matrices; aqua #77D7D1 and teal #33B5A5 for region representations and forecasts; soft lilac #B9A7E8 and violet #7C6CCF for allocation; muted warm ochre #D6A247 and pale peach #F0E0D0 for future-coordinate objects; charcoal #272727 and neutral grey #767676 for text and connectors. Use green or red nowhere because the figure contains no gain/loss direction. Remove the rainbow synthesis strips: replace them with a controlled indigo-to-aqua sequential family. Do not print color codes.

History and forecasting curves: replace plain axis-and-dot line charts with elegant compact multiscale waveform ribbons. Each history curve should combine a smooth trend, medium-frequency seasonality and subtle fine-scale variation, drawn as a dark central stroke with a very narrow translucent same-hue halo and one faint companion trace. The halo is decorative depth, not uncertainty. Remove point markers and heavy Cartesian axes; retain only a short subtle time baseline or directional time cue. Give the five scope-wise forecasts related but visibly distinct multiscale shapes, with clean dark-teal center strokes and pale aqua under-ribbons. Make the final trajectory larger and more refined, with a deep-teal main stroke, a thin pale-aqua echo line and very subtle area tint, no markers and no statistical confidence-band implication.

Future coordinates: completely replace the four ordinary gold line plots with an explicit high-dimensional coordinate visualization. Show future steps tau as an ordered sequence of small ochre points travelling along a smooth folded manifold or helical ribbon inside a clean three-axis projection labelled with tiny coordinate symbols phi_1, phi_2 and phi_3. Next to the manifold, show one compact vertical high-dimensional coordinate vector made of many narrow feature cells, including linear and periodic basis channels. Connect both depictions visually so the manifold is clearly a low-dimensional projection of the high-dimensional coordinate vector. Label this area "Future coordinates". It must immediately read as a coordinate embedding indexed by future step, not as observed time-series data. Keep it flat scientific vector art rather than photorealistic 3D.

Scope matrices: upgrade each simple blue grid into a refined low-rank matrix glyph: layered periwinkle cells with subtle row/column structure, thin charcoal outline and two or three emphasized latent-mode bands. Keep all five matrices aligned and related, without perspective or separate module boxes.

Region descriptors: replace plain segmented gold bars with elegant contiguous region ribbons. Each ribbon should contain subtle coordinate-basis microtextures or small nested feature strips inside each segment. Enforce the scope relation exactly: many tiny regions for s=1; approximately fifteen regions for s=48; five regions for s=144; two regions for s=360; one single region for s=720. Use muted ochre and pale peach only.

Region representations: replace flat cyan bars with layered latent-state tiles. Each region is a compact aqua tile containing several slim internal mode lines or translucent vector bands. Larger scopes must have fewer, wider region tiles. Keep the representation flat and mathematically clean, with no pseudo-3D cubes.

Step-specific synthesis: show one visually shared synthesis operator repeated consistently across rows as a narrow sequence of fine vertical basis strokes transitioning only from indigo through periwinkle to aqua. It must communicate step specificity without a rainbow gradient or a named box.

Condition vector: replace the plain two-color grid with an interleaved latent-feature tensor: periwinkle history channels and ochre coordinate channels arranged as slim vertical feature ribbons that visibly concatenate but remain distinguishable. No extra box around it.

Scope probabilities: preserve the five-row probability field but remove heavy spreadsheet-like cell borders. Use a smooth violet-lilac density field with subtle vertical future-step partitions, thin white separators and normalized-looking variation. Keep the selected five-element probability vector, rendered as five refined proportional discs or narrow capsules in the same scope order.

Typography and geometry: crisp Arial/Helvetica-like sans serif, compact hierarchy, consistent line weights, strict column alignment, generous whitespace and direct labels. Preserve every existing label verbatim and do not add prose. Keep only "Encoder" and "Allocation MLP" as named rounded module boxes.

Invariants: keep "History", "Encoder", "History state", "Future coordinates", "Scope-conditioned forecasting", "Scope matrix", "Region descriptor", "Region representation", "Step-specific synthesis", "Scope-wise forecasts", "Target-conditioned allocation", "Condition vector", "Allocation MLP", "Scope probabilities", "Weighted contraction", "Final trajectory", "s=1", "s=48", "s=144", "s=360" and "s=720" correctly spelled and in their current logical locations. Preserve one shared encoder, one shared architecture, five scope slices and exactly one merge at weighted contraction.

Avoid: generic box-and-arrow flowchart style; rainbow palettes; saturated blue/green/orange combinations; dot markers on time-series curves; ordinary four-line future-coordinate charts; spreadsheet grids; pseudo-3D cubes; glossy gradients; shadows; crossed arrows; new modules; loss terms; BSCA; horizon inputs; training loops; empirical values; legends; captions; logos; watermarks; extra text.
```

## Targeted curve follow-up

```text
Edit Image 1 with one targeted change only. Preserve every label, color assignment, matrix, manifold, coordinate vector, region descriptor, region representation, synthesis strip, probability field, arrow, alignment, scope row, module box, weighted contraction and overall composition exactly as shown.

Change only the graphical treatment of the History curves, the five Scope-wise forecasts and the Final trajectory. Remove every broad translucent filled ribbon, shaded envelope and area-under-curve fill because those shapes could be misread as uncertainty intervals. Replace each with an elegant multiscale line glyph: one crisp dark central waveform, one extremely thin pale same-family echo trace slightly offset from it, and one subtle short high-frequency component woven close to the main line. Keep all traces narrow and line-based with clear white space between them. No dots, no markers, no filled bands, no confidence intervals, no axes beyond the existing faint time baseline. The final trajectory should remain the strongest focal curve through a slightly thicker deep-teal main stroke and one fine aqua highlight line, but it must have no surrounding shaded region.

Do not change anything else. Do not add, remove, move or misspell text. Do not alter the high-dimensional Future coordinates manifold. Do not introduce new colors, modules, arrows, equations, values, legends, captions, logos or watermarks.
```

## Outputs and QA

| Version | Role | Dimensions | SHA-256 |
| --- | --- | --- | --- |
| `figure_iscf_imagegen_refined_v2.png` | full visual refinement | 1,693 × 929 px | `62ff3b86271cc751a69d4d60657a4c9b87a72ad6299205977402d21b4894276a` |
| `figure_iscf_imagegen_refined_v3.png` | recommended curve-safe refinement | 1,693 × 929 px | `5256172c43fbe46e467ee386e522eaa6746cda9e0ffef3273cf369c6986ac90e` |

- Palette: pass for a restrained indigo–periwinkle–aqua–lilac family; no rainbow map or gain/loss colors remain.
- Architecture: pass for the two-lane layout, five scope slices and single weighted-contraction merge.
- Future coordinates: pass as a conceptual high-dimensional coordinate vector plus three-axis manifold projection indexed by future step.
- Intermediate variables: visually upgraded to low-rank matrix glyphs, structured region ribbons, latent-state tiles, sequential synthesis strokes and a softened probability field.
- Text: the major stage labels and five scope labels are present and legible in the recommended v3 draft.
- Claim boundary: the image is a conceptual raster schematic and contains no empirical quantities. The multiple thin curve traces are decorative multiscale glyphs, not uncertainty intervals or multiple predictions.
- Remaining limitation: generated region counts are visually monotonic but not exact for every requested scope; final publication labels, region partition counts and mathematical symbols require deterministic vector reconstruction after the composition is approved.
- `manuscript_replacement`: false.
