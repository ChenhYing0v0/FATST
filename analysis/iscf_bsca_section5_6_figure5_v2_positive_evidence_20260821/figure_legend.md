**Figure 5 | Scope performance across future regions and component-level accuracy gains.** **a**, Percentage excess validation MSE of each sharing scope above the lowest-MSE scope within the same dataset and future region. Results cover scopes $s\in\{1,48,144,360,720\}$ and eight contiguous regions over the 720-step future domain; orange outlines mark the lowest-MSE scope in each dataset--region cell. Values average all available origin--variable validation rows ($n=75{,}607$ for ETTm1 and ETTm2, $n=15{,}127$ for ETTh1 and ETTh2, and $n=95{,}571$ for Weather). **b**, MSE reduction of Full ISCF-BSCA relative to equal scope fusion (`w/o Target-Adaptive Allocation`). **c**, MSE reduction of Full ISCF-BSCA relative to prefix-only training (`w/o BSCA`). Each dataset value in **b,c** averages horizons $\{96,192,336,720\}$; **Mean** denotes the five-dataset macro result. The ablation aggregates use one seed, so no error bars are shown. Source data are provided with the figure.
