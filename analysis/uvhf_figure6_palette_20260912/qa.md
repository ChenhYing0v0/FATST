# Figure 6 配色调整

- 范围：只更新当前投稿图 a/b 的 scope 类别配色，包含同色图例、色条和文字标签。
- 映射：1 蓝 `#47799B`；48 蓝绿 `#40877E`；144 赭橙 `#A97940`；360 紫 `#8570A3`；720 玫瑰 `#B36985`。
- 方法：Python 对现有矢量 PDF 内容流进行精确颜色替换，不重新拟合、采样或绘制数据。
- 验证：内容流操作逐项比对，仅 64 条 RGB 指令变化；其他操作及参数完全相同。使用同一 Python PDFium 渲染器比较，panel c 像素完全一致。
- 原始图保留在 Git 历史中，可由本次修改前版本恢复。
- 静态绘图验证器的字体、SVG、栅格输出规则针对 matplotlib 建图，不适用于此 PDF 颜色流补丁；本次以原字体、原资源、原几何及逐操作比对为验证依据，不为消除静态提示而重绘图片。
- 已人工检查图例映射、曲线可读性和颜色一致性。本次未宣称已通过色觉缺陷模拟。
