# Archived Scripts

这里的脚本是历史实验的 evidence snapshot，不是 active entrypoint。

- `pre-stage-c-reset-20260713/`: Phase0-Phase5 runner、analyzer、sync/check scripts；
- `stage-c-carrier-calibration-20260713/`: 已完成并冻结的 SC0/DAP calibration 工具。

活动脚本只保留在 `scripts/` 与 `scripts/remote/` 顶层。归档脚本中的旧相对路径可能因目录移动而不再
直接可运行；如需复现实验，应从对应 git commit 恢复原布局，而不是在 archive 内隐式启动。
